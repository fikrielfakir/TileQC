* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
    font-size: 12px;
    background-color: #f5f5f5;
    padding: 20px;
}

.form-container {
    max-width: 800px;
    margin: 0 auto;
    background-color: white;
    border: 2px solid #000;
    padding: 0;
}

/* Header Section */
.header-section {
    display: flex;
    border-bottom: 2px solid #000;
}

.header-item {
    flex: 1;
    padding: 10px;
    border-right: 2px solid #000;
    display: flex;
    align-items: center;
    gap: 10px;
}

.header-item:last-child {
    border-right: none;
}

.header-item label {
    font-weight: bold;
    white-space: nowrap;
}

/* Section Styles */
.section {
    border-bottom: 2px solid #000;
}

.section:last-of-type {
    border-bottom: none;
}

.section-header {
    background-color: #d3d3d3;
    padding: 8px 12px;
    font-weight: bold;
    text-align: center;
    border-bottom: 1px solid #000;
    font-size: 11px;
}

.section-content {
    display: table;
    width: 100%;
    border-collapse: collapse;
}

.row {
    display: table-row;
}

.cell {
    display: table-cell;
    border: 1px solid #000;
    padding: 6px 8px;
    vertical-align: middle;
    text-align: center;
    position: relative;
    min-height: 30px;
}

.cell:first-child {
    border-left: none;
}

.cell:last-child {
    border-right: none;
}

/* Specific styling for different sections */
.specification {
    background-color: #f0f0f0;
    font-weight: bold;
}

.specification-value {
    background-color: #f8f8f8;
    font-weight: bold;
    font-size: 11px;
}

/* Silo section specific styling */
.silo-rows .row:first-child .cell:nth-child(4) {
    border-bottom: none;
}

.silo-rows .row:not(:first-child) .cell:nth-child(4) {
    border-top: none;
    border-bottom: none;
}

.silo-rows .row:last-child .cell:nth-child(4) {
    border-bottom: 1px solid #000;
}

/* Input styles */
input[type="text"] {
    border: none;
    background: transparent;
    outline: none;
    font-size: inherit;
    font-family: inherit;
    text-align: center;
    width: 100%;
}

.dotted-line {
    border-bottom: 1px dotted #000 !important;
    min-width: 200px;
    padding: 2px 5px;
}

.dotted-line-small {
    border-bottom: 1px dotted #000 !important;
    min-width: 80px;
    padding: 2px 5px;
    display: inline-block;
}

/* Footer Section */
.footer-section {
    display: flex;
    border-top: 2px solid #000;
    margin-top: 20px;
}

.footer-item {
    flex: 1;
    padding: 15px;
    border-right: 2px solid #000;
    text-align: center;
    font-weight: bold;
}

.footer-item:last-child {
    border-right: none;
}

/* Responsive adjustments */
@media (max-width: 768px) {
    .form-container {
        margin: 10px;
        padding: 0;
    }
    
    body {
        padding: 10px;
    }
    
    .cell {
        font-size: 10px;
        padding: 4px;
    }
    
    .header-item {
        flex-direction: column;
        gap: 5px;
    }
}

/* Print styles */
@media print {
    body {
        background-color: white;
        padding: 0;
    }
    
    .form-container {
        margin: 0;
        box-shadow: none;
    }
}

/* Special styling for merged cells effect */
.colspan-5 {
    text-align: center;
    font-weight: bold;
    background-color: #f0f0f0;
}

/* Header row styling */
.header-row .cell {
    background-color: #e8e8e8;
    font-weight: bold;
    font-size: 10px;
}